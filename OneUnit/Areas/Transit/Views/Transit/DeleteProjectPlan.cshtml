@model ProjectPlanViewModel

@using (Html.BeginForm())
{
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Delete ProjectPlan" })

    <div class="modal-body form-horizontal">
        آیا برای برای حذف اطمینان دارید @Model.Project?
    </div>

    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
}

